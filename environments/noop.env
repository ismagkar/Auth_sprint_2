# Должен всегда быть пустым. Используется в качестве значения по умолчанию для
# пользовательского "локального" `.env`-файла, если он не задан им (пользователем),
# чтобы не было ошибки docker compose при обращении к несуществующему файлу.
#
# Подробности см. в файле `.env.example`.